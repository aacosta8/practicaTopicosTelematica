<p id="notice"><%= notice %></p>
<div style="width:50%; margin:0 auto;">
<div class="w3-container w3-red">
      <h2><%=@publication.name%></h1>
    </div>

    <img src="<%=@publication.photo.url%>"  style="width:100%">

    <div class="w3-container w3-black">
      <h5>Tomada en: <%=@publication.place%></h5>
      <p>Descripcion: <%=@publication.description%></p>
    </div>
<% if user_signed_in? and current_user.id == @publication.user.id%>
<%= link_to 'Edit', edit_publication_path(@publication)%> |
<%= link_to 'Destroy', p, method: :delete, data: { confirm: 'Are you sure?' } %> |
<%end%>
<%= link_to 'Back', publications_path %>
</div>
